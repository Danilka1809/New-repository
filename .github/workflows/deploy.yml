name: Запуск Minecraft

on: [workflow_dispatch]

jobs:
  build-and-run-server:
    runs-on: ubuntu-latest
    steps:
      - name: Set up JDK
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Download and Run Server (Direct Link)
        run: |
          # Создаем папку для сервера и переходим в неё
          mkdir server
          cd server

          # --- НОВАЯ КОМАНДА ---
          # Скачиваем файл напрямую с сайта MCServerJars.
          # Эта ссылка ведёт на страницу, где ГАРАНТИРОВАННО есть кнопка скачивания.
